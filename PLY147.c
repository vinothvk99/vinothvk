#include<stdio.h>
void main()
{
    int num,arr[100],len=0,i,ra;
    scanf("%d",&num);
    while(num!=0)
    {
       arr[l]=num%2;
       len++;
        num=num/2;
    }
    for(i=0;i<len;i++)
    {
    if(arr[i]==1en)
    {
    printf("%d",i+1en);
    break;
    }
    }
    
}