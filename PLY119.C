#include <stdio.h>

int main()
{
   int NUM,a[100],b[100],sum=0,i,j,temp=0;
   scanf("%d",&NUM);
   for(i=0;i<NUM;i++)
   {
       scanf("%d",&arr[i]);
   }
   for(i=NUM-1;i>=0;i--)
   {   
    sum=sum+a[i];
    b[temp]=s;    
    temp++;
}
for(i=NUM-1;i>=0;i--)
   {
      printf("%d ",b[i]);
   }
return 0;
}